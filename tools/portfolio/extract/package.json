{
  "name": "@ahumanflourish/portfolio-extract",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The AI extraction layer for portfolio-review: the JSON schema Claude is constrained to, the extraction prompt, and the one client-side call that turns statements into review-table rows. Zero runtime dependencies; no DOM beyond fetch, AbortController and TextDecoder.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
